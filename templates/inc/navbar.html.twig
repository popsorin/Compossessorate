<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
   <div class="collapse navbar-collapse" id="navbarSupportedContent">
       <ul class="navbar-nav mr-auto">
           <li class="nav-item">
               <a class="nav-link btn btn-dark btn-lg" href="/documents/convert?fullname=asc">Salveaza ca poza</a>
           </li>
           <li class="nav-item">
               <a class="nav-link btn btn-dark btn-lg" href="/document/new">Adauga <span class="sr-only">(current)</span></a>
           </li>
           <li class="nav-item">
               <a class="nav-link btn btn-dark btn-lg" href="#">Login <span class="sr-only">(current)</span></a>
           </li>
           <li class="nav-item">
               <div class="dropdown">
                   <button class="dropbtn nav-link btn btn-dark btn-lg">Numar de documente</button>
                   <div class="dropdown-content">
                       <a href="/documents?documents_per_page=10{{ DocumentsPerPageURL }}">10</a>
                       <a href="/documents?documents_per_page=20{{ DocumentsPerPageURL }}">20</a>
                       <a href="/documents?documents_per_page=30{{ DocumentsPerPageURL }}">30</a>
                       <a href="/documents?documents_per_page=40{{ DocumentsPerPageURL }}">40</a>
                       <a href="/documents?documents_per_page=50{{ DocumentsPerPageURL }}">50</a>
                   </div>
               </div>
       </ul>
       <ul class="navbar-nav ml-auto">
           <li class="nav-item">
                {{ form_start(uploadForm) }}
                {{ form_row(uploadForm.compossessorate_table) }}
           </li>
           <li class="nav-item">
                {{ form_row(uploadForm.submit) }}
                {{ form_end(uploadForm) }}
           </li>
       </ul>
   </div>
</nav>
